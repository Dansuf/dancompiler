VAR
 a b c d e f
BEGIN
  READ a;
  READ b;
  READ c;
  READ d;
  READ e;
  READ f;

  IF a >= b THEN
    WRITE a;
  ELSE
    WRITE b;
  ENDIF
  IF b <= c THEN
    WRITE b;
  ELSE
    WRITE c;
  ENDIF
  IF c > d THEN
    WRITE c;
  ELSE
    WRITE d;
  ENDIF
  IF d < e THEN
    WRITE d;
  ELSE
    WRITE e;
  ENDIF
  IF e = f THEN
    WRITE e;
  ELSE
    WRITE f;
  ENDIF
  IF f <> a THEN
    WRITE f;
  ELSE
    WRITE a;
  ENDIF
END
